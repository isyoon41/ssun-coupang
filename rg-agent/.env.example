# 로컬 개발 기본값: SQLite (별도 DB 설치 없이 바로 실행 가능)
DATABASE_URL="file:./dev.db"

# 운영/스테이징에서는 PostgreSQL 사용을 전제로 한다.
# DATABASE_URL="postgresql://user:password@localhost:5432/rg_agent"
# (전환 시 prisma/schema.prisma 의 datasource provider 를 "postgresql" 로 변경할 것)

AI_PROVIDER="mock"
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
NEXT_PUBLIC_APP_NAME="RG Agent"
